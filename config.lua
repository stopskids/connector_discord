Config = {}

Config.Token = ''
Config.Guild = ''

Config.RateLimit = {
    Enabled = true,
    Requests = 30,
    Interval = 60
}

Config.Cache = {
    Duration = 3600,
    Enabled = true
}